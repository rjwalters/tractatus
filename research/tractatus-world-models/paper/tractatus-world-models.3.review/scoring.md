# Scoring — tractatus-world-models.3

Reviewer: pub-review (claude-fable-5), 2026-07-13. Rubric: `anvil-pub-v2` (9 dimensions, /44, advance ≥35, critical-flag short-circuit). Venue context for calibration: Synthese (declared in `BRIEF.md` frontmatter; no `venue` key in `.anvil.json`, so no venue overlay was scored). Fresh scoring pass; both v2 critical flags independently re-verified against the artifact and a from-scratch compile (see `findings.md`).

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 6 | The v2 blocker is repaired at the artifact, not the prose: Theorem 5.3 — "A Boolean valuation $v : S \to \mathrm{Bool}$ is the profile of some world of $\mathrm{Horn}(S, \mathit{cs})$ iff $v$ satisfies every clause" (— §5, thm:horn-boundary) — now cites `horn\_valuation\_realizable\_iff`, which exists (`proofs/TractatusOntologyHorn.lean:167-176`, verified this pass), states exactly the printed per-valuation boundary, and depends on no axioms per `#print axioms`. The added global-form sentence correctly describes `horn_realizable_iff` (every assignment realizable iff every clause is trivial, lines 137-140). Method (definitions → kernel-checked theorems → historically loaded corollary) is sound throughout; no instance of an artifact-unbacked theorem statement found this pass. |
| 2 | Evidence sufficiency | 6 | 6 | Every headline claim now has its kernel-checked witness: "the spectrum, Horn, equivalence, and exclusion developments comprise 64 declarations" (— §1) — verified exactly this pass (37+9+6+12 declarations, 1,064 lines to the line across the four modules); `lake build` re-run clean (0 errors/warnings, the 9 documented `#eval` info lines); the proof-length claim is corrected and true ("The kernel-checked proof is six lines" — §6, matches the 6 tactic lines of `exclusion_not_horn`). |
| 3 | Clarity of contribution | 5 | 5 | Unchanged strength: contributions enumerated (C1)–(C5), one extractable sentence each, and the central result is crisp in the abstract: "no Horn constraint set is refinement-equivalent to any nontrivial exclusion model" (— abstract). Item (C4) now accurately advertises both boundary forms ("a truth-value profile is realizable iff it satisfies every clause; globally, \emph{every} profile is realizable iff every clause is trivial"). |
| 4 | Related-work positioning | 5 | 5 | Button, Moss, Armstrong/Skyrms, Evans–Berger, McKinsey/Horn, and Benzmüller remain engaged on their actual merits, and the v2 gap is closed: Lokhorst and Weiss are now positioned as the closest precedents — "but those reconstructions are checked by hand" (— §7.5) — and Spinney 2022 (the Synthese venue precedent) is engaged in the §4 footnote as "a complementary, extensional rendering of the same geometry". 17/17 bib entries are cited; no close prior work known to this reviewer is missing. No litsearch sibling exists (legacy baseline; no deduction for absence). |
| 5 | Reproducibility | 5 | 4 | The locator gap is largely closed — "The proof artifact is publicly available in the \lean{tractatus} repository" (— §1) with the URL printed in §1, Appendix A, and the bib note, plus the toolchain pin ("the build is warning-free under Lean~4.26" — Appendix A) and the per-theorem index. One specific weakness remains: no commit hash, tagged release, or archival DOI pins the reviewed state, and the newly cited `horn\_valuation\_realizable\_iff` currently exists only on the unpushed `feature/issue-5` branch — the printed URL does not yet serve the declaration the paper cites (must be resolved at merge, before submission). |
| 6 | Figure & table quality | 4 | 4 | Caption now matches both theorem and artwork: "The Horn and exclusion tiers are not refinement-equivalent" (— Fig. 1 caption), agreeing with the figure's own "no refinement equivalence" label (rendered and inspected this pass: legible, black-only, print-safe). The Appendix booktabs index is clean, with the widest row split across two lines; no instance of a caption that fails standalone found. |
| 7 | Prose & structural quality | 4 | 4 | Flow intact, prose disciplined, and the LaTeX hygiene blocker is verified fixed by an independent from-scratch compile this pass: 0 overfull hboxes (any threshold), 0 errors, 0 undefined references/citations, 10 pages — the `\emergencystretch=3em` preamble fix plus the Appendix module path moved "of the \lean{tractatus} repository" (— Appendix A) onto a breakable layout. No instance of a broken cross-reference or leftover draft marker found (the `\date` is now clean "July 2026"). |
| 8 | Citation hygiene | 5 | 5 | All 17 keys resolve with complete author/title/venue/year fields (verified: cited set == bib set, bibtex 0 errors/warnings); the Hacker claim is now an indirect quotation with no invented locator ("on his telling, Wittgenstein's first philosophy collapsed over its inability to solve this one problem --- colour exclusion" — §1); the TLP 3.42 wording is paraphrased outside quotation marks ("must already have the whole of logical space given with it" — §4); `evans2014` is a proper `@misc` with `eprint`/`archivePrefix`; proper nouns are case-protected against `plain.bst`. |
| 9 | Rhetorical economy | 4 | 4 | Ten pages, every section load-bearing, hedging proportional (the Limitations section concedes real gaps, e.g. binary clauses and fixed atomization), and the sharpest sentences carry the argument: "The 1929 crisis was not that the world exhibits dependence, but that it exhibits dependence of the wrong" \emph{shape} (— §6). A busy reviewer still extracts the contribution from abstract + (C1)–(C5) in under 90 seconds. |
| | **Total** | **44** | **43** | ≥35 threshold met; no critical flags. `advance: true`. |
