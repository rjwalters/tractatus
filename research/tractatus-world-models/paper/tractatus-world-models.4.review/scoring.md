# Scoring — tractatus-world-models.4

Reviewer: pub-review (claude-fable-5), 2026-07-13. Rubric: `anvil-pub-v2` (9 dimensions, /44, advance ≥35, critical-flag short-circuit). First review pass under anvil v0.8.0. Venue context for calibration: Synthese (declared in `BRIEF.md` frontmatter; no `venue` key in `.anvil.json`, so no venue overlay was scored). Fresh scoring pass: v4 is a polish revision of a converged v3 (43/44), and each of the three v4 content changes (artifact hash pinning, companion-title correction, Fig. 1 caption fix) was independently verified against the artifact, plus the new deterministic gates (render gate PASS; artifact_verify `lake build` PASS — see `findings.md`).

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 6 | Unchanged from the converged v3 body: definitions → kernel-checked theorems → historically loaded corollary, with the separation argument stated exactly ("The witness is the \emph{top world} at which every state obtains: positive implications can never rule it out, any exclusion clause must." — §1 (C5)). Theorem 5.3's anchor `horn\_valuation\_realizable\_iff` re-verified at `proofs/TractatusOntologyHorn.lean:167` this pass, and its Lean statement is exactly the printed per-valuation boundary; no instance of an artifact-unbacked theorem statement found. |
| 2 | Evidence sufficiency | 6 | 6 | Every headline claim carries a kernel-checked witness, and the count claims are exact: "the spectrum, Horn, equivalence, and exclusion developments comprise 64 declarations" (— §1) — re-verified 37+9+6+12 declarations and 1,064 lines across the four modules this pass; the external artifact was **run, not just read**: the 0.8.0 artifact_verify gate's `lake build` completed clean (exit 0), and `git diff 0852c5b master -- proofs/` is empty so the result transfers to the pinned commit verbatim. |
| 3 | Clarity of contribution | 5 | 5 | Contributions enumerated (C1)–(C5) with one extractable sentence each, and the central result is crisp in the abstract: "no Horn constraint set is refinement-equivalent to any nontrivial exclusion model" (— abstract). Unchanged by the v4 polish. |
| 4 | Related-work positioning | 5 | 5 | Button, Moss, Armstrong/Skyrms, Evans–Berger, McKinsey/Horn, Benzmüller engaged on their merits; Lokhorst and Weiss positioned as closest precedents ("but those reconstructions are checked by hand" — §7.5) and the Synthese venue precedent engaged in the §4 footnote ("our lattice is a complementary, extensional rendering of the same geometry" — §4 fn.). 17/17 bib entries cited; no close prior work known to this reviewer is missing. No litsearch sibling exists (legacy baseline; no deduction for absence). |
| 5 | Reproducibility | 5 | 5 | The v3 deduction (no immutable locator; cited lemma unpushed) is verifiably closed: "(commit \lean{0852c5b}; the full hash is pinned in Appendix~\ref{app:index})" (— §1), the full 40-char hash printed in Appendix A, and the pin verified this pass as an **ancestor of `master`** with `proofs/`, `lakefile.lean`, `lean-toolchain`, and `lake-manifest.json` all byte-identical since — so the claim "at that commit the build is warning-free under Lean~4.26" (— App. A) is confirmed by the artifact_verify gate's clean `lake build` at HEAD. Toolchain pinned, per-theorem index complete, repo public. |
| 6 | Figure & table quality | 4 | 4 | The v3 caption nit is fixed with the suggested unambiguous phrasing: "no Horn model realizes exactly the image profiles of a nontrivial exclusion model" (— Fig. 1 caption), now matching Theorem 6.5's profile-set-equality content; figure re-inspected in the compiled PDF this pass (legible, black-only); Appendix booktabs index clean. No instance of a caption that fails standalone found. |
| 7 | Prose & structural quality | 4 | 4 | Flow intact and the display discipline is stated and kept ("we display Lean statements where the formal shape is load-bearing and paraphrase elsewhere" — §1); the v4 stray-space fix (`$\mathrm{Im}(\mathrm{freeModel})$`) landed; render gate PASS on the audit-built PDF (10 pages, 0 overfull boxes, 0 placeholders), 0 errors, 0 undefined references. Two mild underfull hboxes (badness ≤2237) are cosmetic and below any actionable bar. |
| 8 | Citation hygiene | 5 | 5 | All 17 keys resolve with complete fields (bibtex 0 errors/warnings; cited set == bib set), and the last known-wrong entry is fixed and verified in-repo: the companion is now cited by its actual title, matching `tractatus-ontology.9/paper.tex` exactly, with the Hacker claim kept as indirect quotation ("on his telling, Wittgenstein's first philosophy collapsed over its inability to solve this one problem --- colour exclusion" — §1) and no invented locators anywhere. |
| 9 | Rhetorical economy | 4 | 4 | Ten pages, every section load-bearing, hedging proportional to the Limitations actually conceded, and the thesis lands in one sentence: "The 1929 crisis was not that the world exhibits dependence, but that it exhibits dependence of the wrong \emph{shape}." (— §6). A busy reviewer extracts the contribution from abstract + (C1)–(C5) in under 90 seconds. |
| | **Total** | **44** | **44** | ≥35 threshold met; no critical flags. `advance: true`. |
